# TODO Master Risks

Record blockers, risks, limitations, and unresolved issues here.

| Date | Risk | Severity | Status | Notes |
|---|---|---|---|---|
| 2026-06-05 | Requested prompt path is missing. | Low | Mitigated | The same master prompt pack is present at repository root; progress records this path adjustment. |
| 2026-06-05 | Prompt lists PHP 8.5, but Boost reports local PHP CLI 8.4. | Low | Open | Laravel 13 tests pass on the installed PHP 8.4 runtime; avoid PHP features unavailable locally. |
| 2026-06-05 | Invite, export, notification, and protected-download feature routes are not implemented yet. | Low | Open | Step 009 establishes the URL contract those later steps must use; concrete link surfaces remain scheduled for their dedicated steps. |
| 2026-06-05 | Demo credentials rely on correct environment configuration. | Medium | Mitigated | The panel and seeder are gated to local/testing/demo and can be disabled with `RUFLO_DEMO_LOGIN_PANEL=false`; production must not run as a safe demo environment. |
| 2026-06-05 | Reminder scaffold has placeholder schema. | Low | Mitigated | Step 054 replaces the placeholder with owner/task links, schedule/lifecycle columns, processing state, factory states, demo data, and browser-triggered processing tests. |
| 2026-06-05 | Future-domain seeders are not implemented. | Low | Open | Step 054 adds reminder seeders. Recurrence, comments, attachments, settings, activity, and invites will need seeders when their real models and ownership columns are introduced in later steps. |
| 2026-06-05 | Demo-login prompt mentions username/email, but this app currently has email-only authentication. | Low | Mitigated | Step 013 documents and tests email as the visible login identifier; a username field should be introduced only through a dedicated future auth/profile step. |
| 2026-06-05 | Form Request helper names can collide with Laravel framework internals. | Low | Mitigated | Step 014 uses `baseRules`, `attributeNames`, and `messageLines` instead of overriding framework methods such as `validationRules`; the 2026-06-06 recheck added direct request-contract coverage. |
| 2026-06-05 | Future-domain custom rules are intentionally absent. | Low | Open | Invite token, recurrence, reminder, upload, import/export, settings, and role rules must be added when those feature surfaces are implemented and testable. Step 015 removed the empty reminder-time placeholder rule and added architecture coverage against empty custom rules. |
| 2026-06-06 | Localization scanner does not prove every literal HTML text node is translated. | Low | Mitigated | Step 016 translates the known visible auth/settings/navigation/dashboard/welcome copy, scans translation/action-message APIs, and now checks static translation-key existence; future full copy audits remain part of accessibility/microcopy/documentation steps. |
| 2026-06-06 | Raw setup database diagnostics could leak through Livewire public state. | Medium | Mitigated | Step 007 now normalizes database inspection failures to a generic marker before assigning public component state, and the setup status test asserts raw SQL details do not render or appear in the snapshot. |
| 2026-06-06 | Maintenance center lacked a normal-user denial boundary. | Medium | Mitigated | Step 008 added `users.is_admin`, the `access-maintenance-center` gate, route middleware, Livewire action authorization, and admin/non-admin tests. Future workspace roles should replace or extend this gate in the collaboration/member steps. |
| 2026-06-06 | The `tag_todo` pivot has no owner/workspace column. | Low | Mitigated | Step 017 keeps project/tag assignment owner-validated in Livewire rules and actions, and `TodoListQuery` now constrains hydrated project/tag relations to the current user so malformed cross-user links do not leak labels. A future collaboration/schema step can revisit whether the pivot should include workspace ownership. |
| 2026-06-06 | Viewer/editor/manager/owner role tests cannot exist before a membership model exists. | Low | Open | Step 019 covers owner/non-owner policy matrices and the current admin-only maintenance gate. Collaboration/member role policies remain scheduled for the later collaboration foundation and member-management steps. |
| 2026-06-06 | Verified route middleware was not enforcing unverified-user redirects. | Medium | Mitigated | Step 020 enabled `MustVerifyEmail` on `User` and added regression coverage for unverified redirects from dashboard, todos, verified settings, setup, security, and maintenance routes. |
| 2026-06-06 | Core task creation could rely on caller-side normalization and mass-assign completion state. | Medium | Mitigated | Step 021 trims titles inside `CreateTodo`, removes `is_completed` from `Todo` fillable fields, updates completion through the explicit lifecycle action, and adds direct action plus mass-assignment regression tests. |
| 2026-06-06 | Task detail pages could leak foreign task metadata through route binding. | Medium | Mitigated | Step 022 avoids implicit todo model binding, uses a numeric route id resolved through `TodoListQuery::findVisibleFor()`, locks the Livewire id, and adds own/foreign detail-page regression tests. |
| 2026-06-06 | Task editing could rely on caller-side normalization. | Medium | Mitigated | Step 023 trims titles inside `UpdateTodo`, keeps edit mutations detail-only, and adds action plus Livewire edit regression tests for normalization, foreign ids, invalid input, and archived tasks. |
| 2026-06-06 | Generic completion toggling could blur complete versus reopen activity and skip events during bulk completion. | Medium | Mitigated | Step 024 replaces the toggle action/event with explicit complete/reopen actions and events, updates Livewire methods and labels, and makes bulk completion reuse `CompleteTodo` instead of a mass update. |
| 2026-06-06 | Task archive reversal could be confused with deleted-task restore and bulk archive could skip events. | Medium | Mitigated | Step 025 uses explicit unarchive naming for task archive reversal, reserves restore wording for future trash/project contexts, and routes bulk archive/unarchive through single-task actions with event/idempotency tests. |
| 2026-06-06 | Permanent task deletion could destroy private data without a stronger recovery and authorization design. | Medium | Mitigated | Step 026 keeps `forceDelete` denied and absent from the UI, implements soft-delete restore instead, and adds tests proving permanent delete stays unavailable. |
| 2026-06-06 | Lifecycle rules could drift across separate action classes as more task surfaces are added. | Medium | Mitigated | Step 027 centralizes accepted source states and target buckets in `TodoLifecycleStateMachine`, keeps action classes as mutation boundaries, and adds state-machine regression coverage. |
| 2026-06-06 | Project detail pages could leak foreign project names or task lists if routed through implicit binding or raw model lookup. | Medium | Mitigated | Step 028 resolves project detail pages through `ProjectListQuery::findVisibleFor()`, locks the Livewire project id, keeps the route behind `auth` and `verified`, and adds owner/foreign/empty/detail-link regression tests. |
| 2026-06-06 | Whitespace-only tag names could normalize to an empty stored label if Livewire's basic required rule accepted spaces. | Medium | Mitigated | Step 029 adds `TagName`, uses a translated validation message, guards `CreateTag` against direct empty normalized names, and adds Livewire/action regression tests. |
| 2026-06-06 | Invalid direct priority values could silently become Normal if validation was bypassed. | Medium | Mitigated | Step 030 uses Laravel enum validation in Livewire forms, makes `TodoData::fromArray()` throw a translated validation error for invalid provided priorities, and adds direct DTO/action regression coverage. |
| 2026-06-06 | Direct due-date values could rely on broad PHP date parsing or invalid date rollover if Livewire validation was bypassed. | Medium | Mitigated | Step 031 adds the reusable `DueDate` rule, uses it in the Livewire form, reuses its parser in `TodoData::fromArray()`, and adds invalid-date regression tests. |
| 2026-06-06 | Due-date buckets are not per-user timezone aware yet. | Low | Open | Step 031 documents and tests the current app-timezone behavior. Per-user timezone handling remains scheduled for the language/timezone settings step. |
| 2026-06-06 | A focused Today page action could mutate an owned task outside the Today result set if it reused the general task lookup. | Medium | Mitigated | Step 032 adds `TodoListQuery::findTodayFor()` and uses it for Today completion, so forged ids for tomorrow, overdue, completed, archived, trashed, or foreign tasks are not found through that page. |
| 2026-06-06 | A focused Overdue page action could mutate an owned task outside the overdue result set if it reused the general task lookup. | Medium | Mitigated | Step 033 adds `TodoListQuery::findOverdueFor()` and uses it for Overdue completion, so forged ids for today, upcoming, completed, archived, trashed, or foreign tasks are not found through that page. |
| 2026-06-06 | A focused Upcoming page action could mutate an owned task outside the upcoming result set if it reused the general task lookup. | Medium | Mitigated | Step 034 adds `TodoListQuery::findUpcomingFor()` and uses it for Upcoming completion, so forged ids for today, overdue, no-due-date, completed, archived, trashed, or foreign tasks are not found through that page. |
| 2026-06-06 | Non-numeric project/tag query-string filters could be ignored and widen a searched list. | Medium | Mitigated | Step 035 routes unexpected non-empty project/tag URL values through the same empty-result behavior as foreign or missing numeric ids, and `TaskSearchTest` covers the tampered search case. |
| 2026-06-06 | Invalid lifecycle, priority, or due-bucket filter values could be ignored and widen a filtered list. | Medium | Mitigated | Step 036 carries invalid scalar filter state through `TodoFilters::hasInvalidFilter`, resolves it to an empty owner-scoped query result, and covers invalid priority, due, and lifecycle state in `TaskFilterTest`. |
| 2026-06-06 | Sort chip icons can break rendering if the component emits an icon name Flux does not provide. | Low | Mitigated | Step 037's first focused test run caught `icon.arrow-up-down` as unavailable; the chip metadata now uses the installed `adjustments-horizontal` Flux icon and `TaskSortingTest` covers rendered sort/order chips. |
| 2026-06-06 | Saved views can contain stale project or tag ids after another workspace changes. | Medium | Mitigated | Step 038 stores only normalized criteria and applies saved views through the same owner-scoped filter sanitizer. Foreign, deleted, archived, or missing resource ids resolve to the existing empty filtered state without leaking names or widening results. |
| 2026-06-06 | Direct bulk action calls can include foreign, missing, or non-actionable ids. | Medium | Mitigated | Step 039 keeps Livewire validation strict, re-scopes direct action calls to owner/actionable rows, authorizes every actionable row, and reports skipped ids through `BulkActionResult` so skipped work is visible instead of silently looking successful. |
| 2026-06-06 | Kanban drag/drop ordering would imply persistence that the schema does not support yet. | Low | Mitigated | Step 040 uses reliable Livewire fallback buttons for status/project movement and documents that drag ordering should wait for a per-user position field and ordering tests. |
| 2026-06-06 | Calendar reminder and recurrence lanes cannot render real entries yet. | Low | Open | Step 054 adds owned reminder data and a dedicated reminders page. Calendar integration and recurrence models remain deferred to the recurrence steps. |
| 2026-06-06 | Checklist items are not independently restorable after direct item deletion. | Low | Mitigated | Step 042 treats checklist rows as contained task details, not standalone tasks. Parent archive/trash preserves rows, but deleting one checklist row is immediate; task-level Trash remains the recovery boundary until a future activity/audit feature designs item-level recovery. |
| 2026-06-06 | Shared template visibility does not grant cross-user access yet. | Low | Open | Step 043 stores and displays shared visibility, but policies and queries keep templates owner-only because collaboration members and viewer/editor/manager/owner roles do not exist yet. The later collaboration/member steps must replace the owner-only shared-template boundary with explicit role checks. |
| 2026-06-06 | Inbox triage is manual only. | Low | Open | Step 044 intentionally adds fast capture and manual triage without reminders, smart cleanup, or automation. Later reminder, smart-view, automation, and web-processing steps must add web-triggered follow-up behavior if Inbox cleanup becomes long-running or scheduled. |
| 2026-06-06 | Focus timer is browser-only and not persisted. | Low | Mitigated | Step 048 adds owner-scoped persisted Pomodoro session state with start, pause, resume, complete, abandon, and active-session resume behavior. Step 049 remains responsible for broader time tracking semantics. |
| 2026-06-06 | Full habit streak tracking is not part of Step 046. | Low | Mitigated | Step 047 adds owner-scoped habits, check-in history, honest daily/weekly progress, current/best streak math, factories, seeders, docs, and tests. |
| 2026-06-06 | Goal edit/delete/archive UI is intentionally narrow. | Low | Open | Step 046 adds create, milestone check-in, and task-link behavior needed for progress. Later polish or dashboard/report steps can add rename/archive/delete flows with matching policy and recovery decisions. |
| 2026-06-06 | Habit history editing and backfill are not exposed yet. | Low | Open | Step 047 only toggles today's check-in to avoid spoofed historical streaks. Future reporting or settings work can add owner-scoped history editing with explicit validation and audit behavior. |
| 2026-06-06 | Pomodoro countdown does not advance server-side when the browser page is closed. | Low | Open | This is intentional for restricted hosting. The active session row persists and can be resumed, but exact countdown progression depends on the open browser tab or an explicit user action. Broader time accounting belongs to Step 049. |
| 2026-06-06 | Active time tracking is explicit-action based. | Low | Open | Step 049 stores one running row and resumes it on `/todos/time`, but final duration is committed only when the user stops or discards the timer through a web action. A very small concurrent double-start race remains because single-active enforcement is application-level rather than a database partial unique index. |
| 2026-06-06 | Archived blockers still count as unresolved. | Low | Open | Step 050 excludes archived waiting tasks from blocked smart views, but an archived blocker task still keeps a dependency open until completed or explicitly removed. This preserves explicit dependency context and avoids hidden automatic unblock behavior. |
| 2026-06-06 | Cleanup smart views are heuristic review aids. | Low | Open | Step 051 derives stale, unplanned, blocked, and risky buckets from current task metadata; it does not automatically clean up, notify, archive, or mutate tasks. Later automation/reminder/dashboard steps must add explicit web-triggered workflows if cleanup should perform writes. |
| 2026-06-06 | Automation rules use a feature-specific bounded runner before the generic processor exists. | Low | Closed | Step 052 records progress, remaining work, dry runs, disabled no-ops, retry, and resume by re-running owner-scoped chunks. Step 053 replaced the feature-specific chunk loop with a reusable manual web-processing engine shared by later long-running features. |
| 2026-06-06 | Manual web-processing processes must provide their own owner scope. | Low | Open | Step 053 centralizes chunking and progress, but the generic runner intentionally does not infer ownership for arbitrary models. Each `ManualWebProcess` implementation must start from an authenticated owner or policy-approved boundary, and tests must cover multi-user isolation for each feature consumer. |
| 2026-06-06 | Reminder delivery is browser-triggered, not exact-time. | Low | Open | Step 054 processes due reminders on dashboard open, reminders page open, or manual process button. This preserves restricted-hosting compatibility but cannot deliver exactly at `remind_at` while the user is away. |
| 2026-06-06 | Inline local-notification Alpine payloads can exceed Livewire's morph-aware Blade compiler regex limits. | Medium | Mitigated | Step 054 stabilization moved the large browser-reminder object into a view-scoped `@once` helper, left a compact `window.RuFlo.localReminderNotifications(...)` call in the card, rebuilt native datepicker assets, and added focused/full regression evidence. |
