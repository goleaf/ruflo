# MASTER PARALLEL DESIGN PROMPT

You are upgrading the Laravel Todo app design.

Read:

- `GLOBAL_DESIGN_RULES.md`
- `PARALLEL_MERGE_MATRIX.md`
- the assigned file from `parallel-prompts/`

You must work only on your assigned prompt.

Do not ask the user for confirmation.

Do not change unrelated business logic.

Use Laravel 13, latest Livewire, Flux v2, Tailwind CSS 4, and clean SCSS where useful.

Do not use Volt.

Use free/open-source/self-hosted/browser-native design features only.

The final app must work beautifully on:

- mobile
- tablet
- desktop

Every prompt must create its own report file:

`DESIGN_AGENT_REPORT_XX.md`

Never mark another design prompt complete.
