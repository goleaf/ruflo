# TODO Master Decisions

Record important architecture/product decisions here.

| Date | Area | Decision | Reason |
|---|---|---|---|
| 2026-06-05 | Prompt location | Use the root `MASTER_PROMPT.md` and `steps/` files because `docs/todo-master-plan/MASTER_PROMPT.md` is not present in this checkout. | The prompt pack exists at the repository root and contains the complete 100-step sequence. |
| 2026-06-05 | Runtime | Code against the installed Laravel Boost runtime: Laravel 13.14, Livewire 4.3, Flux 2.14, Tailwind 4.3, Pest 4.7, PHP CLI 8.4. | Local installed versions are authoritative for tests and generated code. |
| 2026-06-05 | Step continuation | Treat existing commits `b69ac76`, `e53b67c`, `2149412`, and `b461fae` as completed baseline work for Steps 001, 002, 003, and 004. | Docs, changelog, tests, and git history already describe and verify the foundation, stack, Livewire/Flux, and no-Volt baseline. |
| 2026-06-05 | Worktree recovery | Do not commit placeholder reminder/notification files until they are implemented and tested. | The interrupted staged batch contained generated skeletons that are not stable product behavior. |
| 2026-06-05 | Frontend assets | Keep Tailwind CSS 4 in `resources/css/app.css` and add SCSS as a separate `resources/scss/app.scss` Vite entry. | This preserves Tailwind/Flux conventions while allowing clean shared tokens, accessibility helpers, reusable surface effects, and print styles. |
| 2026-06-05 | Restricted hosting | Default normal runtime to `QUEUE_CONNECTION=sync` and web-triggered chunked processing. | Shared hosting cannot assume workers, cron, supervisors, shell access, or Artisan access for normal usage. |
| 2026-06-05 | Web setup | Provide a protected status-only setup page before any web updater execution controls. | This gives owners deployment visibility without exposing a public installer or risky migration runner. |
| 2026-06-05 | Maintenance center | Implement only bounded cache/view cleanup actions before the generic processing engine exists. | Broader retry/resume processors, demo reset, and storage cleanup are already planned later and should not be improvised unsafely. |
| 2026-06-05 | Domain readiness | Treat `https://ruflo.test` as the configured root for tracked defaults, tests, and local Herd runtime. | Generated redirects, signed links, storage URLs, and future invite/export/download links need a stable HTTPS host instead of implicit localhost or HTTP fallbacks. |
| 2026-06-05 | Demo login | Render demo credentials only from a safe-environment, config-backed, database-confirmed catalog and submit quick logins through Fortify. | The login page needs local/demo usability without bypassing auth protections or exposing known credentials in production-like environments. |
| 2026-06-05 | Factory inventory | Scope Step 011 to committed models only: User, Project, Tag, Todo, and the tracked placeholder Reminder. | The reminder factory can prove model creation, but meaningful reminder states remain deferred until the planned reminder step adds owner, schedule, lifecycle, and processing columns. |
| 2026-06-05 | Seeder inventory | Seed concrete committed workspace models and make the current demo seeders idempotent; do not seed placeholder Reminder rows yet. | The app needs immediate local/demo usability without duplicating records, creating known-password users in production-like environments, or creating global placeholder records with no owner or task relationship. |
| 2026-06-05 | Demo login identifier | Show the seeded demo users' email addresses as the login identifier instead of adding usernames. | The current users table and Fortify configuration authenticate by email only, so introducing a username field would expand scope beyond the demo panel. |
| 2026-06-05 | Request validation | Treat auth Form Request classes as the canonical rule source for Fortify registration and password reset actions. | Fortify passes arrays to action contracts, so sharing request-class rules avoids inline action validation while preserving Fortify's normal flow. |
| 2026-06-05 | Livewire validation | Keep Livewire-only task/profile validation in Livewire form objects or components unless a separate HTTP route/controller exists. | Livewire form objects are the correct boundary for component state and avoid forcing HTTP Form Requests into non-HTTP interactions. |
| 2026-06-05 | Ownership validation | Reject forged project, tag, and selected todo ids at the Livewire validation boundary while keeping action-level owner re-scoping. | Users get clear validation feedback and direct action calls remain safe if validation is bypassed. |
| 2026-06-05 | Future validation rules | Do not create placeholder invite, recurrence, reminder, upload, import/export, settings, or role rules before those stable domains exist. | Rules need concrete callers and tests; placeholders create false coverage and may fight later feature designs. Step 015 recheck removed the empty reminder-time placeholder rule. |
| 2026-06-06 | Localization | Resolve Livewire title attributes as translation keys through the shared head partial when a matching language line exists. | PHP attributes cannot call translation helpers directly, and the shared layout can translate keys without changing Livewire routes. |
| 2026-06-06 | Progress tracking | Expand root progress and checklist files to one explicit line per step from 001 through 100. | The current prompt forbids compressed ranges and requires every step to have its own status trail. |
| 2026-06-06 | Setup diagnostics | Store only generic database inspection failure markers in public Livewire setup status state. | Livewire serializes public state into the browser snapshot, so raw connection exceptions must not be exposed even on protected setup pages. |
| 2026-06-06 | Maintenance authorization | Use `users.is_admin` and an `access-maintenance-center` gate for the protected maintenance center until the later workspace/member role system exists. | Step 008 requires normal users to be denied, and the current app has no workspace or role model yet. |
| 2026-06-06 | Private workspace boundary | Keep the owning `User` as the private workspace boundary for Step 017 and do not add a separate `Workspace` model yet. | Current todo, project, and tag resources already have direct `user_id` ownership; a separate workspace table would add migration and authorization scope before collaboration/member features exist. |
| 2026-06-06 | Dashboard privacy | Read dashboard counters through `DailySummaryQuery::for($user)` and owner-scoped query objects. | Dashboard counts are private data and must follow the same scoped query boundary as todo lists, search, filters, and project/tag pickers. |
| 2026-06-06 | Tampered filters | Return an empty scoped todo result for foreign, archived, or missing project/tag filter IDs. | Ignoring a bad numeric filter could fall back to a broader list, while applying it could let another user's ID influence the current user's query. Empty results preserve privacy and make the tampered state obvious. |
| 2026-06-06 | Authorization roles | Do not add placeholder viewer/editor/manager/owner roles during Step 019. | The current app has no workspace membership model; adding role stubs before collaboration would create false policy coverage and likely conflict with the planned invite/member steps. |
| 2026-06-06 | Request validation tests | Keep translated Form Request contract tests in the feature suite. | The unit suite does not boot Laravel's translator container, and these request classes intentionally expose translated attributes and messages. |
| 2026-06-06 | Email verification enforcement | Implement `MustVerifyEmail` on `User` and keep dashboard, todos, appearance, setup, security, and maintenance behind `verified`; leave profile settings auth-only. | Routes and docs already required verified private pages, but Laravel's verified middleware only enforces that contract for models implementing `MustVerifyEmail`. Profile remains reachable so unverified users can see their verification status and manage account details. |
| 2026-06-06 | Core task creation boundary | Keep task creation in `CreateTodo`, assign ownership only through the user's relationship, trim titles at the action write boundary, re-scope organization ids inside the action, and keep lifecycle/system fields out of todo mass assignment. | Livewire validation improves feedback, but backend callers must still be safe. Creation should not be able to complete, archive, delete, or cross-link a task by mass assignment or forged ids. |
| 2026-06-06 | Private task detail lookup | Implement task detail pages as class-based Livewire pages that accept a numeric id but resolve the task through `TodoListQuery::findVisibleFor()` instead of implicit model binding. | A raw implicit binding would load the record before owner scoping. The query object gives the same not-found behavior used by list actions and avoids leaking another user's title or metadata. |
| 2026-06-06 | Task editing boundary | Keep task editing in `UpdateTodo`, trim titles at the action write boundary, re-scope project/tag ids inside the action, reject archived edits, and keep completion/archive/delete state in separate lifecycle actions. | Editing should update details only. Lifecycle transitions need their own authorization, events, tests, and user feedback so the app does not blur complete, archive, restore, and delete behavior. |
| 2026-06-06 | Task completion boundary | Use explicit `CompleteTodo` and `ReopenTodo` action classes, `TodoCompleted` and `TodoReopened` events, and state-specific Livewire methods while preserving the compact row checkbox UI. | A generic toggle action hid two different lifecycle transitions and one generic event was not enough for later activity/reminder rules. Separate transitions keep policy, events, labels, and tests precise without changing the fast UI. |
| 2026-06-06 | Task archive return path | Name task archive reversal as unarchive in actions, policy abilities, Livewire methods, translations, and bulk workflows. | The plan step title says archive and restore, but task archive reversal is not the same as future soft-delete restore. Explicit unarchive naming avoids mixing archive reversal with trash recovery while preserving project restore behavior. |
| 2026-06-06 | Task trash behavior | Support restore from Trash with `RestoreDeletedTodo`, `TodoRestoredFromTrash`, `TodoPolicy::restore`, and owner-scoped `findTrashedFor()` while keeping `forceDelete` denied. | Soft delete recovery is useful and safe for restricted hosting, but permanent deletion needs a stricter product and authorization design before exposing irreversible controls. |
| 2026-06-06 | Lifecycle state machine | Keep lifecycle transition rules in `TodoTransition` and `TodoLifecycleStateMachine`, with action classes delegating guard decisions before mutation. | Separate action classes remain readable, while one transition map prevents complete/archive/delete/restore meanings from drifting apart as later steps add more task surfaces. |
| 2026-06-06 | Project detail routing | Implement project detail as a class-based Livewire page that accepts a numeric id but resolves through `ProjectListQuery::findVisibleFor()`. | Implicit project binding would load a project before owner scoping. The query boundary gives the same not-found privacy behavior used by task detail pages and keeps archived projects reviewable without making them assignment targets. |
| 2026-06-06 | Tag browsing | Make tag badges link to the existing owner-scoped todo tag filter instead of adding a separate tag detail page or bulk tag workflow in Step 029. | The current requirement is scoped labels, assignment, filtering, and validation. A filter link keeps labels useful now while leaving bulk tag assignment and richer label views to their later dedicated workflow steps. |
| 2026-06-06 | Priority validation | Use Laravel's backed-enum validation rule for task priorities and keep direct DTO priority parsing strict when a value is provided. | The enum already owns the allowed priority values. Validation should reject unexpected values at the UI boundary and direct action boundary instead of letting invalid data silently become Normal. |
| 2026-06-06 | Priority sorting | Generate priority sort SQL from `App\Enums\Priority` weights instead of hardcoding a separate query-only weight map. | Filtering, badges, translations, factories, seeders, and sorting should all mean the same priority order as later task surfaces are added. |
| 2026-06-06 | Due-date format | Store and accept task due dates as canonical `Y-m-d` date-only values through `App\Rules\Todos\DueDate`. | Browser date inputs, the database date column, factories, seeders, filters, and direct action calls need one strict format instead of accepting natural-language dates through PHP parsing. |
| 2026-06-06 | Due-date timezone | Compare due-date buckets in the configured app timezone, currently UTC, until per-user timezone settings are implemented. | The app has no user timezone field yet. A documented app-timezone contract is safer than pretending account-specific date boundaries exist. |
| 2026-06-06 | Today route | Add `todos.today` as a dedicated class-based Livewire page instead of only relying on `todos.index?due=today`. | The main filtered list remains useful, but a focused Today route gives dashboard/navigation targets and tests a stable current-task workflow while reusing the same owner-scoped query boundary. |
| 2026-06-06 | Today actions | Resolve Today page mutations through `TodoListQuery::findTodayFor()` instead of the broader `findVisibleFor()`. | A forged Livewire call should not let the focused Today page complete an owned task that is not active and due today, even though the main task workspace can handle that task elsewhere. |
| 2026-06-06 | Overdue route | Add `todos.overdue` as a dedicated class-based Livewire page instead of only relying on `todos.index?due=overdue`. | Overdue work needs a stable, focused review route and dashboard target while still reusing the same owner-scoped date query boundary as the main task list. |
| 2026-06-06 | Overdue actions | Resolve Overdue page mutations through `TodoListQuery::findOverdueFor()` instead of the broader `findVisibleFor()`. | A forged Livewire call should not let the focused Overdue page complete an owned task that is not active and past due. |
| 2026-06-06 | Upcoming route | Add `todos.upcoming` as a dedicated class-based Livewire page instead of only relying on `todos.index?due=upcoming`. | Future-dated work needs a stable planning route and dashboard target while still reusing the same owner-scoped date query boundary as the main task list. |
| 2026-06-06 | Upcoming actions | Resolve Upcoming page mutations through `TodoListQuery::findUpcomingFor()` instead of the broader `findVisibleFor()`. | A forged Livewire call should not let the focused Upcoming page complete an owned task that is not active and due after today. |
| 2026-06-06 | Search engine | Keep Step 035 search as owner-scoped, bound SQL `LIKE` with escaped wildcards instead of adding a hosted search service or full-text index. | The current app needs private title search, SQLite/local compatibility, and restricted-hosting safety without paid services, workers, or external daemons. |
| 2026-06-06 | Search query parameters | Treat non-numeric project/tag URL values as invalid filters that produce an empty result, matching foreign or missing numeric filter ids. | Ignoring an unexpected query parameter could widen a searched list; empty results preserve the user's explicit filter state without leaking private data. |
| 2026-06-06 | Filter invalid state | Add `TodoFilters::hasInvalidFilter` and have `TodoListQuery` resolve invalid lifecycle, priority, and due-bucket filter state to an empty result. | The Livewire component owns URL-state parsing, but the query object should remain the final read boundary that prevents invalid filter state from widening private task lists. |
| 2026-06-06 | Sort stability | Keep sorting in `TodoListQuery::applySort()`, allow-list sort keys through `TodoFilters`, normalize direction to `asc` or `desc`, and add deterministic secondary ordering to every sort path. | Sort state arrives from the URL and must never become raw user SQL. Stable secondary ordering keeps paginated private task lists predictable when primary sort values tie. |
| 2026-06-06 | Saved task views | Store saved views as per-user normalized criteria JSON and apply them by writing bounded values back through the existing Livewire URL-state and `TodoListQuery` sanitizer. | Saved views should not duplicate task results, store foreign resource names, or create a second filter engine. Reusing the current owner-scoped query path keeps stale ids and tampered values private and predictable. |
| 2026-06-06 | Bulk action reporting | Return `BulkActionResult` from bulk action classes and render selected/affected/skipped/failed counts in the Livewire UI. | Livewire validation rejects invalid selected ids, while direct action calls may safely skip foreign or non-actionable ids. A structured result makes that behavior explicit without introducing queues or background workers. |
| 2026-06-06 | Kanban movement fallback | Build Step 040 board movement with Livewire buttons and project selects instead of drag/drop ordering. | The app has no per-user position field yet. Button fallback is reliable on mobile and keyboard, preserves lifecycle rules, and avoids creating an unpersisted ordering system before the manual-ordering step exists. |
| 2026-06-06 | Calendar scope | Build Step 041 around active task due dates, strict month navigation, and no-due-date visibility; show reminder and recurrence lanes as translated placeholders until owned schedule/rule schemas exist. | The current reminder table has no owner or schedule fields and recurrence models do not exist. Rendering fake entries would create false product behavior and privacy assumptions before the dedicated reminder/recurrence steps. |
| 2026-06-06 | Checklist scope | Model Step 042 subtasks as contained `todo_checklist_items`, not full child tasks. Parent task lifecycle controls checklist visibility and mutability. | Checklists need progress and ordering now without prematurely adding independent project/tag/due/reminder/archive/trash behavior that belongs to later dedicated task, reminder, recurrence, dependency, and collaboration steps. |
| 2026-06-06 | Template sharing scope | Store `todo_templates.visibility` as `private` or `shared`, but keep all template reads and instantiation owner-only until collaboration members and roles exist. | Step 043 needs private/shared template states without inventing a fake role system. Owner-only policies prevent leaks now, while the stored visibility field gives later collaboration steps a concrete migration point. |
| 2026-06-06 | Inbox state | Store quick-capture state on `todos.inbox_captured_at` instead of inferring Inbox membership from missing project or due date. | Users can intentionally keep normal active tasks unprojected or unscheduled. An explicit timestamp gives the Inbox a clear query boundary, sort key, factory state, seeded demo state, and triage completion marker. |
| 2026-06-06 | Focus scope | Build Focus mode as a derived owner-scoped active-task view, not a stored focus-list model. Include all urgent tasks first, then fill normal slots with overdue, due-today, and high-priority tasks. | The current app already has priority and date signals. A derived query avoids new persistence before manual ordering, goals, timer, and time-tracking steps exist, while ensuring urgent work is not hidden by an arbitrary limit. |
| 2026-06-06 | Goals and milestones | Model Step 046 with owner-scoped `goals`, owner-scoped `goal_milestones`, and nullable task links to a goal/milestone instead of turning goals into projects or checklists. | Goals need measurable progress across existing tasks and optional project context without duplicating project/list behavior or faking child tasks. Nullable links preserve tasks if a goal or milestone is removed later. |
| 2026-06-06 | Goal progress | Calculate goal progress only from real linked tasks and checked-in milestones; empty goals show 0%. | Honest progress avoids fake stats. Users can see exactly which completed tasks and milestone check-ins contribute, and seeded demo data links existing tasks rather than inventing progress-only rows. |
| 2026-06-06 | Habit scope | Keep full habit tracking out of Step 046 and treat milestone check-in as the only check-in behavior here. | Step 047 is the dedicated habits tracker. Adding streak models now would collapse two plan steps and create a second partially designed routine system beside task templates. |
| 2026-06-06 | Habit model | Model Step 047 with owner-scoped `habits`, owner-scoped `habit_check_ins`, optional goal links, and nullable task links to a habit. | Habits need check-in history and streaks without overloading goals, templates, or recurring tasks. Nullable task links preserve tasks if a habit is removed later. |
| 2026-06-06 | Habit progress | Calculate habit progress and current/best streaks only from real check-in dates. Daily habits count today's check-in; weekly habits count unique check-in days in the current week against the stored target. | Honest streaks avoid fake counters and do not require cron. The UI can explain exactly which period is being counted from stored check-in rows. |
| 2026-06-06 | Habit check-in boundary | Step 047 only toggles today's check-in from the web UI; it does not expose arbitrary history backfill or scheduled streak maintenance. | Today-only writes reduce spoofing risk and keep restricted hosting support simple. Future reporting/history steps can add explicitly audited date editing if needed. |
| 2026-06-06 | Pomodoro scope | Model Step 048 as owner-scoped `pomodoro_sessions` linked to focus tasks, not as full time tracking. | Step 048 needs task-linked Pomodoro resume and timer state, while Step 049 is the dedicated time-tracking step. Keeping only session status, elapsed seconds, and linked task avoids inventing billing/reporting semantics early. |
| 2026-06-06 | Pomodoro runtime | Keep Pomodoro timing browser-ticked and persist only web-triggered Livewire transitions. | Restricted hosting cannot assume cron, workers, supervisors, or server-side timers. A persisted active session gives resume state without background infrastructure. |
| 2026-06-06 | Time tracking model | Model Step 049 as owner-scoped `time_entries` linked optionally to tasks, projects, and completed Pomodoro sessions. | Time tracking needs durable private totals and provenance without inventing collaboration, billing, reporting, or background processing semantics before their dedicated steps. |
| 2026-06-06 | Time tracking runtime | Keep manual logs, timer start/stop/discard, deletion, and Pomodoro conversion as bounded web-triggered Livewire/action workflows. | Restricted hosting cannot assume cron, workers, supervisors, terminal access, or Artisan commands during normal usage. A persisted running row gives resume state while explicit web actions save final duration. |
| 2026-06-06 | Task dependencies | Model Step 050 as owner-scoped `todo_dependencies` edges from a waiting task to a private blocker task. | Explicit edges are easier to validate, seed, test, and later extend than inferring "waiting" from tags or status names. |
| 2026-06-06 | Blocked state runtime | Derive blocked state at read time from open dependency rows and blocker completion state instead of storing a separate task status. | Completing or removing blockers becomes immediately visible without cron, queue workers, supervisors, Artisan commands, or a background unblock process. |
| 2026-06-06 | Cleanup smart views | Model Step 051 cleanup as derived owner-scoped active-task views rather than new cleanup storage. | Stale, unplanned, blocked, and risky reviews can be computed from existing task, project, tag, due-date, priority, and dependency data without background work or schema churn. |
| 2026-06-06 | Cleanup URL state | Treat invalid cleanup view parameters as an empty result and normalize invalid sort/direction to safe fallbacks. | URL-backed state must not widen private task queries or become raw SQL. A fail-closed view makes tampering obvious while keeping allowed sorts useful. |
| 2026-06-06 | Automation runtime | Build Step 052 automation rules as owner-scoped, browser-triggered Livewire actions with feature-specific bounded chunks and run logs; keep the reusable cross-feature processor for Step 053. | The step needs useful automation now without cron, workers, terminals, or hosted services. A feature-specific runner can show progress, retry, and resume by re-running the owner-scoped chunk while avoiding premature generic infrastructure. |
| 2026-06-06 | Manual web-processing engine | Centralize chunk size, request runtime, detail-limit, dry-run, and remaining-count mechanics in `RunManualWebProcess`, while each feature supplies an owner-scoped `ManualWebProcess` query and one-record mutation. | Shared mechanics reduce duplicated long-operation loops without creating a global data scope that could weaken feature-specific authorization rules. |
| 2026-06-06 | Habit creation route | Keep habit creation on a separate class-based `habits.create` Livewire page while the habit index owns tabs, check-ins, progress, and task linking. | Splitting creation keeps the tracker page focused, preserves no-Volt conventions, and lets validation/action responsibilities remain explicit in architecture tests. |
| 2026-06-06 | Reminder runtime | Process due reminders from authenticated dashboard/page/manual browser actions and write database notifications only. | Restricted hosting cannot promise cron-like exact-time delivery, and Step 055 owns the broader notification center. Database-only delivery keeps Step 054 self-hosted and testable without email or paid providers. |
| 2026-06-06 | Notification links | Treat notification action URLs as same-host display hints, not authorization decisions. | Notification payloads can become stale or malformed. The center hides external/protocol-relative/unsupported links, pre-checks known task links, and still relies on destination routes to re-check private record access. |
| 2026-06-06 | Daily dashboard runtime | Replace scheduled daily-summary delivery with an authenticated dashboard card backed by `DailyDashboardQuery`. | Restricted hosting cannot rely on cron, workers, or email. A browser-rendered owner-scoped query keeps daily counters private, fresh on page open, and testable without background infrastructure. |
| 2026-06-06 | Daily summary runtime | Render daily summaries on the authenticated dashboard through `DailyDashboardQuery` instead of scheduled emails or background jobs. | Restricted hosting cannot assume cron, workers, supervisors, terminal access, or email providers. A web-rendered summary keeps counts private, fresh on visit, and testable without external services. |
| 2026-06-06 | Dashboard foundation widgets | Aggregate dashboard foundation counters through `DashboardFoundationQuery` and render browser-native Flux widgets instead of cached background summaries or external charts. | The dashboard needs owner-scoped Today, Overdue, Upcoming, Priority, Reminder, Recurrence, Goal, Habit, Project, and Time signals while preserving restricted-hosting behavior and avoiding paid charting services. |
| 2026-06-06 | Dashboard widget customization | Store foundation widget visibility and order as small Livewire session arrays validated against a server-owned widget key allow-list. | Step 062 needs user customization without introducing a dashboard preference table, background processor, or shared state. Session preferences change only presentation and never alter `DashboardFoundationQuery` owner-scoped counters. |
| 2026-06-06 | Project progress dashboard | Aggregate project/list progress through `ProjectProgressDashboardQuery` during authenticated dashboard renders instead of storing dashboard snapshots or adding background jobs. | Step 063 needs accurate owner-scoped project health, no-project cleanup signals, and archived/deleted/foreign exclusions while preserving restricted-hosting behavior and avoiding a second project read path in the Livewire view. |
| 2026-06-06 | Reports overview read boundary | Aggregate reports through `ReportsOverviewQuery` during authenticated reports renders instead of storing report snapshots, adding a cache table, or using external chart services. | Step 064 needs owner-scoped productivity, overdue, habit, project, and time totals while preserving restricted-hosting behavior and keeping private reporting data out of background infrastructure. |
| 2026-06-06 | Local browser charts | Normalize chart row data through `BrowserBarChart` and render dashboard/report bars with `x-ui.local-bar-chart` instead of Flux chart components, external chart libraries, CDNs, hosted analytics, or paid charting services. | Step 065 needs accessible chart output that is self-hosted, browser-native, testable, and safe for restricted hosting without adding runtime services or JavaScript chart dependencies. |
| 2026-06-06 | Activity history storage | Store owner-scoped `activity_records` from synchronous domain-event listeners with subject snapshots and allow-listed metadata instead of queues, scheduled logs, or background audit jobs. | Step 066 needs private task activity history that works on restricted hosting, survives deleted subjects, avoids leaking arbitrary model payloads, and remains queryable through a dedicated owner-scoped read boundary. |
| 2026-06-06 | Activity history UI | Render `/activity` from `ActivityFeedQuery` in a class-based Livewire page with bounded load-more pagination and stale subject-link filtering. | The activity surface should show useful history without background processing, implicit binding leaks, or links to deleted/private records the current owner cannot still access. |
| 2026-06-06 | Task timeline UI | Embed task history with a class-based `TaskTimeline` component that re-resolves the parent task through `TodoListQuery`, reads records through `ActivityFeedQuery::forTodo()`, and shares `ActivityFormatter` with `/activity`. | Step 067 needs consistent activity copy on task detail pages without creating another formatter, widening the activity read scope, or rendering stale subject links for deleted/private records. |
| 2026-06-06 | Collaboration membership model | Keep project ownership on `projects.user_id` and store only non-owner manager, editor, and viewer access in `project_memberships`. | Owner access should be derived, not duplicated. A single `ProjectAccess` resolver keeps project policies, shared task policies, and future invite/member workflows aligned without replacing the private workspace model. |
| 2026-06-06 | Link-only invites | Store project invite links as encrypted tokens plus token hashes, generate temporary signed `https://ruflo.test` URLs, and accept them through an authenticated generic Livewire page before creating membership. | The app must avoid email dependencies, avoid exposing project data before acceptance, support cancellation/expiration/used states, and remain safe on restricted hosting with normal web requests only. |
| 2026-06-06 | Member management boundary | Resolve member role edits and removals from active project membership ids, then authorize through `ProjectMembershipPolicy` before delegating to actions. | Step 070 must not trust browser-submitted ids or duplicate role checks in Blade. Active-row re-querying keeps removed, foreign, and stale memberships from widening shared project access. |
| 2026-06-06 | Member management role updates | Edit project member roles through `UpdateProjectMemberRole` with scoped active-membership lookup and assignable non-owner roles only; remove members by setting `removed_at` through `RemoveProjectMember`. | Owner access is derived from `projects.user_id`, so the owner must not be editable or removable as a membership row. Scoped lookup plus action-level authorization keeps forged membership ids and removed members from widening access. |
| 2026-06-06 | Shared task read scope | Let task search, safe project filters, Today/Overdue/Upcoming/blocked lists, task summaries, and task-derived dashboard counters read private owned tasks plus active shared project tasks. | Step 071 needs collaboration to appear in normal planning surfaces, but only active memberships on active projects where the task owner is the project owner should widen read access. |
| 2026-06-06 | Shared task mutation boundary | Keep trash, bulk actions, tag labels/filters, owner-only assignment pickers, and deleted-task access private while rendering shared viewer rows as read-only. | Shared members need useful visibility without turning viewer access into mutation rights or leaking private organization labels. Editors and managers still rely on `TodoPolicy` for permitted shared task actions. |
| 2026-06-06 | Recurrence rule scope | Step 057 stores one owner-scoped rule per active task and reserves occurrence generation, exceptions, and series editing for the next dedicated recurrence steps. | Restricted hosting cannot rely on a scheduler, and rule definitions must be stable before web-triggered generation can safely prevent duplicates with `last_generated_until`. |
| 2026-06-06 | Recurrence generation runtime | Generate recurring task occurrences through `RunManualWebProcess` from authenticated browser actions, with generated task metadata on `todos` and a unique owner/rule/date key. | Restricted hosting cannot rely on cron, workers, or terminal commands. Generated task rows need to be ordinary private tasks while duplicate prevention and resume state remain enforceable at the database and processing layers. |
| 2026-06-06 | Recurrence occurrence skip behavior | Treat soft-deleted generated occurrences as already generated when checking owner/rule/date duplicates. | Deleting one generated task should act as an intentional skip for that occurrence date and should not be recreated by a later retry, resume, or cursor reset before the dedicated exception model exists. |
| 2026-06-06 | Recurrence exception model | Store skipped, moved, and edited occurrence exceptions in `todo_recurrence_exceptions` keyed by owner, rule, and original occurrence date. Moved occurrences change the generated task `due_date` but preserve `recurrence_occurs_on` as the original series date. | One exception row per original date gives a stable audit trail and lets generation skip both skipped originals and moved adjusted dates without rewriting the source series or requiring a scheduler. |
| 2026-06-06 | Recurrence edit scope | Split generated occurrence editing into explicit occurrence-only and series-from-here actions. Series edits update the source title/priority and future generated rows only when those rows have not already been individually changed. | A modal scope choice prevents accidental series rewrites. Dates remain controlled by recurrence rules or occurrence moves, while edited/moved/skipped exceptions stay authoritative. |
